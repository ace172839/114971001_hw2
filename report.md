1. 方法特性比較
本次作業透過基礎與進階測資，深入對比了傳統 NLP 方法（TF-IDF, Rule-Based）與現代生成式 AI（Gemini-2.0-Flash）在文本處理上的本質差異。

傳統方法的優勢與限制：
傳統方法的核心優勢在於「高可解釋性」與「極致的運算效率」。在 A-1 的相似度計算中，TF-IDF 矩陣的運算僅需毫秒級，且我們可以清楚追溯分數來源於哪些關鍵詞的重疊。然而，其限制在「缺乏語意理解」與「泛化能力差」。這在進階測資中尤為明顯，例如句子「今天天氣很不好，不適合出去運動」，傳統方法因與正面句子共享大量詞彙（天氣、適合、運動），容易誤判為高相似度，無法處理「不」字帶來的語意反轉。此外，A-2 的規則分類器在面對未定義的詞彙（如擴充測資中的「慢跑」）時直接失效，顯示了窮舉規則的不可能性。

AI 方法的優勢與限制：
現代 AI 的優勢在於「強大的語意推理」與「靈活性」。B-2 的實作顯示，Gemini 不僅能識別「慢跑」屬於運動，更能處理進階測資中「混合情感」的複雜語句（例如：「介面設計還行，但核心功能崩潰」），準確判斷出整體為負面情感，這是單純計算正負詞頻無法做到的。B-3 的生成式摘要則展現了優秀的重組與潤飾能力，產出的文字通順流暢。然而，AI 的劣勢在於「不可控性」與「成本」。API 的延遲遠高於本地運算，且輸出格式偶爾不穩定（如實作中遇到的 JSON 解析問題），需要額外的工程手段來確保穩定性。

2. 實作心得
在本次實作過程中，我深刻體會到從「傳統工程」轉向「提示工程」的思維變革。

遇到的困難與突破：
在實作 A-3 的 AdvancedStatisticalSummarizer 時，我嘗試解決傳統摘要「斷章取義」的問題。為了過濾無意義的高頻詞（如語助詞），我設計了結合「詞性標註」與「段落 IDF」的演算法，試圖找出貫穿全文的主題詞。這個過程非常繁瑣，需要反覆調整數學權重（如首尾句加權、詞頻懲罰）。
相比之下，實作 B-3 的 AI 摘要時，困難點轉移到了如何設計精準的 Prompt，以及如何處理 API 的 Rate Limit 和錯誤重試（Retry Logic）。

理解與感受：
傳統方法像是在「教電腦規則」，我們必須告訴它「跑步是運動」、「好是正面」，程式的智慧上限取決於我們定義規則的詳盡程度。
而 AI 方法則像是在「與一位博學的助理溝通」，它已經具備了通用知識，我們只需要引導它運用這些知識來解決特定問題。
進階測資的測試讓我看到，雖然 AI 在處理隱喻、反諷或複雜邏輯上完勝傳統方法，但在某些需要精確關鍵字匹配的場景下，傳統方法依然是最穩健的選擇。

未來的學習方向：
本次作業讓我對 RAG（檢索增強生成）產生了濃厚興趣。我意識到單純依賴 AI 生成可能會產生幻覺，而單純依賴傳統檢索又缺乏理解力。未來希望學習如何將 TF-IDF 或向量檢索（Vector Search）作為 AI 的「外掛記憶」，結合兩者優點。

3. 應用建議
基於量化數據與質性觀察，我提出以下技術選型建議：

適合使用傳統方法的情況：
大數據初步篩選：當需要從海量文檔中快速檢索候選集時，TF-IDF 是成本最低且最高效的選擇。
高頻交易或即時系統：在對延遲極度敏感（毫秒級）的場景，傳統算法不可替代。
封閉域的簡單分類：若任務僅涉及特定的、有限的關鍵字（如特定的工單分類），規則式方法更易於維護且除錯容易。

適合使用 AI 方法的情況：
處理非結構化與模糊語意：如社交媒體的情感分析，用戶用語充滿變體與隱喻，AI 能提供高準確率。
內容生成與改寫：如自動摘要、文案撰寫，需要考慮文法通順與起承轉合。

結合策略：
最佳實踐應是「傳統、AI兩者交叉使用、相輔相成」。

上個月參加黑客松比賽，我也自己製作了一個「旅遊智能助手」、「智能圖片分析助手」。
在製作初期，我原以為可以輕鬆當個 AI API 工程師，然而事與願違。
AI 在判斷意圖、分析圖片上，雖然已經有了非常顯著的進步，但在面對一些語焉不詳的句子會出現答非所問；而且最重要的一點是，API 的 response time 經常超過 5 秒，
這對於使用者體驗來說，是個非常致命的問題，沒有一個客戶會願意看著 pending 的圓圈不停旋轉。
因此我先在 local 端進行篩選，在使用者絮絮叨叨的描述中，抓出幾個關鍵 keyword，讓 LLM 知道這段話的主題可能是什麼，並且設計良好 prompt 讓 LLM 只回答與「旅遊
相關」的內容，其他的「悲慘童年」、「與旅伴吵架」...等內容直接無視，讓 LLM 能專注於判讀核心內容。
在進行這些改善後，平均一則回覆的時間縮短至大約 4.2 秒，是個勉強可以接受的數字。
